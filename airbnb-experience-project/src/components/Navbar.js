// We need to export each component to use it in other file
export default function Navbar() {
    return (
        <nav>
            <img nameClass="navbar--logo" src="airbnb-logo.png" />
        </nav>
    )
}